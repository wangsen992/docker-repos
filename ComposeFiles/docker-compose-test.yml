---
version: "3.6"
services:
  test:
    build:
      context: /mnt/d/docker-repos/
      dockerfile: OS/ubuntu/Dockerfile # where setup files are installed.
      args:
        - BASE=ubuntu:devel
        - DI_RESOURCES=Resources
        - IMAGE_RESOURCES=/app/Resources
        - USER=wangsen992
        - INSTALL_APP_SCRIPT=
    image: wangsen992/ubuntu_dev
    container_name: test
    security_opt:
      - seccomp:unconfined #optional
    environment:
      - TZ=Etc/UTC
      - NVIDIA_VISIBLE_DEVICES=all
      - CONTAINER=test
    volumes:
      - /mnt/d:/mnt/d
    restart: "no"
    command: sudo tail -F /var/log/*
